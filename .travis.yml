language: php

php:
  - 5.6

before_script:
  - cp .env.travis .env
  - mysql -e 'create database homestead_test;'
  - composer self-update
  - COMPOSER_ROOT_VERSION=2.0.x-dev composer install --no-interaction --prefer-source --dev

script:
  - vendor/bin/phpunit